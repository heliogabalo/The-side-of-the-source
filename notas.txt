http://www.intel.la/content/www/xl/es/support/detect.html?iid=dc_iduu
http://ark.intel.com/es-es/products/36463/Intel-Core2-Duo-Processor-E7300-3M-Cache-2_66-GHz-1066-MHz-FSB#@downloads



TODO:


- localizar .config gedit

- Supervision diaria:
 * -reglas locales udev
	 -es buena idea hacer un log e ir apuntando las pesquisas.
	 

A3-- dynamicDBG.md Referencias de comandos del lenguaje 111 p

- Hay que averiguar qué es, para que sirve, como funciona, por qué está ahí
	el directorio `/boot/i386/`

	* Pero sobre todo _POR QUÉ ESTÁ AHÍ_
	
	*averiguar pcscs-- unkown grup udevd (on lab)	
	
- yenta module hace referencia al bus ISA
-NFS
-LVM:
 * Una correcta instalacion  del hipervisor, aconseja la instalaci'on tanto
   del dom0 como de las vms atrav'es del gestor LVM.
 * Preparacion de los medios con "lvm-cachedVolumes"
 * Preparacion del nucleo
 * Configuracion basica:
 		- puente de red para el dom0.
 * Los drivers de los dispositivos, siguen utilizando el bus ISA, aún cuando
 la configuración del nucleo elude cualquier mención a ellos.
 En este sistema NO EXISTE el _bus ISA_. 
---

- Disco de arranque
	_ref. el torito_
	- desde cero
		- cear dispositivo
			- planificar el espacio 
				- tamaño de "bloque"; del dispositivo de bloque
				- tamaño de sector, o FS que alojara
		- dar formato para el FS
			- Otros _sistemas de archivo_
			- Los _wrapers_
		- instalar los archivos necesarios
	- copia desde otro medio "arrancable"
		- commprobar el espacio `file`
		- copiar sin los simbolos de depuracion `objcopy --strip-all`
		- copia exacta `dd <if=> <of=> <bs=> <count=>` _o clon_
		- instalar los archivos necesarios
	- Diferencias en una instalacion sobre HDD
		- el grub o gestor de arranque
		- imagen comprimida
		- `step by step` _En dos pasos_
			- sysV
			- sistemd initrd, `change_root`
		- Systemd init, `pivot_root`
		
		
